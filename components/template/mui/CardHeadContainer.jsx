import { CardHeader } from "@mui/material";

export default function CardHeadContainer({
    cardHeadProps
}) {
    return (
        <CardHeader
            {...cardHeadProps}
        />
    )
}
