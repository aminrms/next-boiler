import { Checkbox } from "@mui/material";

export default function CheckboxContainer({
    checkBoxProps
}) {
    return (
        <Checkbox
            {...checkBoxProps}
        />
  )
}
