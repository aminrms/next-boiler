import { FormControlLabel } from "@mui/material";

export default function FormControlLabelContainer({
    formLabelProps,
}) {
  return (
      <FormControlLabel
          {...formLabelProps}
      />
  )
}
